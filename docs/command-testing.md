# Command testing

Test commands with harmless targets first, verify permission checks, and confirm failures are surfaced clearly. State-changing commands should not silently continue after validation errors.