# Configuration notes

Keep machine-specific configuration outside the repository when possible. Document required environment variables, safe defaults, and platform-specific prerequisites without committing secrets.